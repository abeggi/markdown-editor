# Gravity Editor

## Toolbar

fissa e Copy Code *attivo*.

```
console.log("DokuWiki Style!");
```

```bash
#!/bin/bash
# Avvia l'app usando il virtual environment
cd "$(dirname "$0")"
if [[ ! -d venv ]]; then
  echo "Crea prima il virtual environment: python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
  exit 1
fi
exec ./venv/bin/python app.py "$@"
```

satollo